目标：收据 OCR → “草稿记录”生成与确认流程（最小实现）。
范围：
- Bot 接收图片或 PDF，保存到临时存储，调用本地 tesseract 适配层抽取文本
- 服务端依据关键字（合计/Amount Due/应付等）打分生成金额候选、尝试识别币种、解析日期
- 生成“草稿记录”，Bot 回显候选并让用户点选确认；确认后入库（复用 L1 写接口）
- 清理：临时文件有 TTL；草稿超时自动作废
请：给出可替换 OCR_PROVIDER 的接口边界；提供失败与重试路径；pytest 针对解析器核心逻辑。
