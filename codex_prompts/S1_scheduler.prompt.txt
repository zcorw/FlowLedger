目标：实现“定时提醒/自动记账回执”最小可用增量（S1）
范围：
- SQL：按 docs/sql/modules/scheduler.sql.md 建表与触发器
- API：
  - POST /v1/jobs 创建提醒任务（title, schedule/cron, channel=telegram）
  - POST /v1/confirmations 回执（用于触发自动记账：与 L1 集成）
  - GET /v1/job-runs 查询任务实例/历史
- Worker：最小扫描器（按 schedule 生成实例并下发到 Bot）
- Bot：接收提醒卡片的回调并展示“一键入账”按钮
- 测试：任务创建/扫描/回执的关键 happy path

请：
1) 列出将新增/修改的文件路径树；
2) 提供迁移脚本、API 路由、扫描器任务（可定时触发/手动命令）、Bot 回调、测试与 cURL；
3) 仅改动与本任务直接相关的文件。

